[pytest]
testpaths = tests
# Keep pytest cache outside the checkout. Direct pytest execution reads this file
# before .env or Makefile exports are loaded.
cache_dir = ~/.tony-ai/pytest
markers =
    concurrency: multiprocess and persistence race tests
    mcp: JSON-RPC contract tests for the kernel MCP server
    integration: tests that exercise multiple kernel components
    e2e: end-to-end workflow tests
